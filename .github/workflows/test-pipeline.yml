name: Project Syntax Checks

on:
  push:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check project files
        run: |
          echo "Checking internship projects..."
          ls -la
          echo "Pipeline completed successfully!"
